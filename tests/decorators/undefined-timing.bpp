#!/usr/bin/env bashy
// errorcheck
// ERROR "EDECO-UNDEF"
# bash++ profile: decorators · phase S197 · status: planned
#
# Reference: CPython test_decorators.py test_errors — Python raises NameError
# while `def` executes when the decorator name is unresolved. dhnt
# difference: registration SUCCEEDS (per-call late resolution; the native
# registry and advice rules may load later); the first call fails with
# EDECO-UNDEF, status 1. The echo below proves registration-time silence.

@missing()
func lonely() {
    echo "lonely:body"
}

echo "registered:ok"
lonely()
