#!/usr/bin/env bashy
// run
# bash++ profile: decorators · phase S197 · status: supported
#
# The call spelling is intentionally varied before otherwise ordinary Go-style
# declarations: positional value, keyword argument, and an empty decorator
# call. Reserved dotted names have their own negative fixture.

@n("audit")
func valueDecorated() {}

@n(level: "debug")
func keywordDecorated() {}

@n()
func emptyDecorated() {}
