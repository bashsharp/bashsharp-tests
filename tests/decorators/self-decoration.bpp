#!/usr/bin/env bashy
// errorcheck
// ERROR "EDECO-SELF"
# bash++ profile: decorators · phase S197 · status: supported
#
# dhnt: self-decoration is rejected at REGISTRATION (EDECO-SELF), by rule and
# independent of binding order. Python contrast: `@self` on `def self` fails
# only because the name is unbound when `def` executes; with a prior binding
# it silently decorates the old object. The echo proves the failure happens
# at the declaration, not at a later call.

echo "before:registration"

@self()
func self(c *Call) {
    c.Next()
}
